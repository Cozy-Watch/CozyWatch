# Coding preferences

- Prefer early returns over deeply nested conditional rendering.
- Keep state updates and data transformations immutable.
- Favor simple, readable code over unnecessary abstraction.
